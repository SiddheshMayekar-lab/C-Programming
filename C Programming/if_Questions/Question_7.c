
// 7. Year check
#include <stdio.h>
int main() {
    int year;
    printf("Enter year: ");
    scanf("%d", &year);
    if (year > 2000)
        printf("Year is greater than 2000\n");
    return 0;
}
